import { SiteFooter } from 'mrc';

// Site footer: brand lockup, navigation, contact details, fine print, and the
// admin/dashboard link. Captured wide so the multi-column desktop layout shows.
export const Default = () => <SiteFooter />;
